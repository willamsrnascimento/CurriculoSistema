@model SistemaCurriculos.Models.Objetivo;

<h3>Editar Objetivo</h3>


<div class="row">
    <div class="col-md-4">
        <form asp-controller="Objetivo">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <input type="hidden" asp-for="Id" />
            <input type="hidden" asp-for="CurriculoId" />

            <div class="row">
                <div class="input-field col s6">
                    <input asp-for="Descricao" id="Descricao" class="validate" />
                    <label for="Descricao">Descricao</label>
                    <span asp-validation-for="Descricao" class="red-text darken-3"></span>
                </div>
            </div>

            <div class="row">
                <input asp-action="Editar" type="submit" value="Atualizar" class="btn btn-small green darken-1" /> &nbsp;
                <a asp-action="Detalhe" asp-controller="Curriculo" asp-route-id="@Model.CurriculoId">Voltar</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}